---
# Replace ansible_host with your real VM address.
# worker_instances controls how many worker processes run on the VM.
# worker_cpu_budget / worker_mem_budget may be overridden per host.
all:
  children:
    workers:
      hosts:
        worker-vm-1:
          ansible_host: 167.99.38.141
          ansible_user: root
          worker_instances: 2
          worker_cpu_budget: 4
          worker_mem_budget: 3000
